{
  "description": "SN Burst Pipeline Configuration",
  "version": "2.0",
  
  "input_data": {
    "cc_sample_folder": "/path/to/cc/cluster_images",
    "es_sample_folder": "/path/to/es/cluster_images",
    "file_pattern": "*_planeX.npz",
    "comment": "Folder containing cluster image NPZ files from one SN burst"
  },
  
  "sample_selection": {
    "n_es_events": 325,
    "n_cc_events": 3300,
    "comment": "Number of events to select from each sample type"
  },
  
  "neural_networks": {
    "main_track_identifier": {
      "model_path": "/path/to/mt_identifier.keras",
      "threshold": 0.5,
      "comment": "Main track identification CNN - trained on cluster images"
    },
    "channel_tagger": {
      "model_path": "/path/to/channel_tagger.keras",
      "threshold": 0.5,
      "enabled": false,
      "comment": "Channel tagging CNN - trained on volume images; set enabled=true to run"
    },
    "electron_direction": {
      "model_path": "/path/to/electron_direction.keras",
      "enabled": false,
      "comment": "Electron direction predictor (to be implemented later)"
    }
  },
  
  "volume_creation": {
    "online_pointing_utils_path": "../submodules/online-pointing-utils",
    "create_volumes_script": "python/app/create_volumes.py",
    "volume_config_json": "json/volume_creation.json",
    "output_folder": "selected_volume_images",
    "comment": "Volume creation from selected main track clusters"
  },
  
  "output": {
    "base_folder": "/path/to/output",
    "metrics_json": "metrics.json",
    "report_pdf": "pipeline_report.pdf",
    "save_intermediate": true,
    "comment": "Output paths for results and reports"
  },
  
  "processing": {
    "shuffle_clusters": true,
    "random_seed": 42,
    "verbose": true
  }
}
